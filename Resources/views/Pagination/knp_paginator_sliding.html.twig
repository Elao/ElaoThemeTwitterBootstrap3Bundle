{% if pageCount > 1 %}
    {% if first is defined %}
    <ul class="pagination">
        <li class="first {{ current != first ? '' : 'disabled' }}">
            <a href="{{ path(route, query|merge({(pageParameterName): first})) }}" title="{{ 'interface.first'|trans }}" {{ tooltip() }}>
                {% block first_btn %}
                    <span class="glyphicon glyphicon-step-backward"></span>
                {% endblock %}
            </a>
        </li>
    </ul>
    {% endif %}

    <ul class="pagination">
        <li class="previous {{ previous is defined ? '' : 'disabled' }}">
            {% set previous_url = previous is defined ? path(route, query|merge({(pageParameterName): previous})) : '#' %}
            <a href="{{ previous_url }}" title="{{ 'interface.previous'|trans }}" {{ tooltip() }}>
                {% block prev_btn %}
                    <span class="glyphicon glyphicon-chevron-left"></span>
                {% endblock %}
            </a>
        </li>

        {% for page in pagesInRange %}
        <li class="{{ page != current ? '' : 'active' }}">
            <a href="{{ page != current ? path(route, query|merge({(pageParameterName): page})) : '#' }}">
                {{ page }}
            </a>
        </li>
        {% endfor %}

        <li class="next {{ next is defined ? '' : 'disabled' }}">
            {% set next_url = next is defined ? path(route, query|merge({(pageParameterName): next})) : '#' %}
            <a href="{{ next_url }}" title="{{ 'interface.next'|trans }}" {{ tooltip() }}>
                {% block next_btn %}
                    <span class="glyphicon glyphicon-chevron-right"></span>
                {% endblock %}
            </a>
        </li>
    </ul>

    {% if last is defined %}
    <ul class="pagination">
        <li class="last {{ current != last ? '' : 'disabled' }}">
            <a href="{{ path(route, query|merge({(pageParameterName): last})) }}" title="{{ 'interface.last'|trans }}" {{ tooltip() }}>
                {% block last_btn %}
                    <span class="glyphicon glyphicon-step-forward"></span>
                {% endblock %}
            </a>
        </li>
    </ul>
    {% endif %}
{% endif %}
